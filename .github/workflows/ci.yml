name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up JDK 25
      uses: actions/setup-java@v4
      with:
        java-version: '25'
        distribution: 'temurin'

    - name: Set up JavaFX
      run: |
        $javafxUrl = "https://download2.gluonhq.com/openjfx/25.0.1/openjfx-25.0.1_windows-x64_bin-sdk.zip"
        $javafxDir = "C:\Program Files\JavaFX\javafx-sdk-25.0.1"
        if (-not (Test-Path $javafxDir)) {
            Invoke-WebRequest -Uri $javafxUrl -OutFile javafx.zip
            Expand-Archive -Path javafx.zip -DestinationPath "C:\Program Files\JavaFX" -Force
            Remove-Item javafx.zip
        }

    - name: Run Tests
      run: mvn test
      env:
        JAVA_HOME: C:\Program Files\Eclipse Adoptium\jdk-25
